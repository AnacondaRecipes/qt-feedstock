Index: root/qtwebengine/src/3rdparty/chromium/build/config/BUILDCONFIG.gn
===================================================================
--- work.orig/qtwebengine/src/3rdparty/chromium/build/config/BUILDCONFIG.gn
+++ work/qtwebengine/src/3rdparty/chromium/build/config/BUILDCONFIG.gn
@@ -56,7 +56,7 @@ if (target_cpu == "") {
     # build for ARM by default, not the host_cpu (which is likely x64).
     # This allows us to not have to specify both target_os and target_cpu
     # on the command line.
-    target_cpu = "arm"
+    target_cpu = "arm64"
   } else {
     target_cpu = host_cpu
   }
Index: work/qtwebengine/src/3rdparty/chromium/build/toolchain/mac/BUILD.gn
===================================================================
--- work.orig/qtwebengineg/src/3rdparty/chromium/build/toolchain/mac/BUILD.gn
+++ work/qtwebengine/src/3rdparty/chromium/build/toolchain/mac/BUILD.gn
@@ -480,9 +480,16 @@ template("mac_toolchain") {
   }
 }
 
+mac_toolchain("clang_arm64") {
+  toolchain_args = {
+    current_cpu = "arm64"
+    current_os = "mac"
+  }
+}
+
 mac_toolchain("clang_arm") {
   toolchain_args = {
-    current_cpu = "arm"
+    current_cpu = "arm64"
     current_os = "mac"
   }
 }
@@ -514,7 +521,7 @@ mac_toolchain("clang_x86_v8_arm") {
     current_os = "mac"
 
     if (defined(v8_current_cpu)) {
-      v8_current_cpu = "arm"
+      v8_current_cpu = "arm64"
     }
   }
 }
Index: work/qtwebengine/src/3rdparty/chromium/third_party/lzma_sdk/CpuArch.h
===================================================================
--- work.orig/qtwebengine/src/3rdparty/chromium/third_party/lzma_sdk/CpuArch.h
+++ work/qtwebengine/src/3rdparty/chromium/third_party/lzma_sdk/CpuArch.h
@@ -27,7 +27,7 @@ MY_CPU_LE_UNALIGN means that CPU is LITT
 #if defined(MY_CPU_AMD64) \
     || defined(_M_IA64) \
     || defined(__AARCH64EL__) \
-    || defined(__AARCH64EB__)
+    || defined(__AARCH64EB__) || defined(__arm64__)
   #define MY_CPU_64BIT
 #endif
 
@@ -62,7 +62,7 @@ MY_CPU_LE_UNALIGN means that CPU is LITT
     || defined(__LITTLE_ENDIAN__) \
     || defined(__ARMEL__) \
     || defined(__THUMBEL__) \
-    || defined(__AARCH64EL__) \
+    || defined(__AARCH64EL__) || defined(__arm64__) \
     || defined(__MIPSEL__) \
     || defined(__MIPSEL) \
     || defined(_MIPSEL) \
